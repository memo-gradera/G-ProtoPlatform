/**
 * Database enum constants — re-exported from Prisma for use outside generated client imports.
 * Values align with @proto-platform/contracts and apps/web workflow rules.
 */
export {
  AppRoleName as APP_ROLE,
  UserStatus as USER_STATUS,
  IdeaStatus as IDEA_STATUS,
  IdeaPriority as IDEA_PRIORITY,
  PrototypeStatus as PROTOTYPE_STATUS,
  ReviewDecision as REVIEW_DECISION,
  AttachmentEntityType as ATTACHMENT_ENTITY_TYPE,
  AuditEntityType as AUDIT_ENTITY_TYPE,
} from "@prisma/client";

export type {
  AppRoleName,
  UserStatus,
  IdeaStatus,
  IdeaPriority,
  PrototypeStatus,
  ReviewDecision,
} from "@prisma/client";
